# Copy to .env next to docker-compose.yml.
# Credentials can also be set per-device in the app's Settings page instead.
TZ=Europe/Amsterdam

# TP-Link Tapo account (for the P110 energy plug)
TAPO_EMAIL=
TAPO_PASSWORD=

# Dreo account (for the humidifier, cloud API)
DREO_EMAIL=
DREO_PASSWORD=
